
  
#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	int n_last;
 	n_last = n%10;
	if (n_last>5)
	{
		printf("Last digit of n is %d and is greater than 5\n", n_last);
	}
	else if (n=0)
	{
		printf("Last digit of n is %d and is 0\n", n_last);
	}
	else
	{
		printf("Last digit of n is %d and is less than 5\n", n_last);
	}
	       
	   
	return (0);
}
